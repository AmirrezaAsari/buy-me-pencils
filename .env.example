PORT={PORT}

DB_NAME={DB_NAME}
DB_USER={DB_USER}
DB_PASSWORD={DB_PASSWORD}
DB_HOST={DB_HOST}
DB_PORT={DB_PORT}

ADMIN_SETUP_TOKEN={ADMIN_SETUP_TOKEN}

# --- Crypto Donation Module (USDT TRC20) ---

# AES-256 encryption secret for private keys (min 32 chars)
ENCRYPTION_SECRET=your-32-char-minimum-secret-key-here

# TronGrid API (default: https://api.trongrid.io)
TRON_FULL_HOST=https://api.trongrid.io

# Optional: TronGrid API key for higher rate limits
# TRONGRID_API_KEY=your-trongrid-api-key

# Master wallet for sweeping received USDT (required for sweep)
TRON_MASTER_ADDRESS=your-master-tron-address
TRON_MASTER_PRIVATE_KEY=your-master-private-key